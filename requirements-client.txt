# Local "trigger" client dependencies. Run on a laptop with no GPU/CUDA.
#
# This installs the lightweight stack needed to:
#   - submit ACI jobs via scripts/run_job.py
#   - validate input images locally before paying for an ACI run
#   - tail and download job logs
#
# The heavy ML/runtime stack (torch, trimesh, diffusers, etc.) lives in
# requirements-runtime.txt and is installed only inside the Docker images.

# Azure SDKs
azure-identity~=1.15
azure-mgmt-containerinstance~=10.1
azure-mgmt-appcontainers~=3.1
azure-storage-blob~=12.19
azure-storage-file-share~=12.16
azure-keyvault-secrets~=4.7

# Logging (matches utils/logger.py). Loose lower bounds -- the API surface
# we use is stable, and tight pins clash with newer venvs that may have
# heavier libraries (e.g. rembg pulling pillow >= 12).
loguru>=0.7
rich>=13

# Local image validation (used by the --validate-only flag)
pillow>=10
uvicorn[standard]
fastapi>=0.95
